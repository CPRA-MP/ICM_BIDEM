Included in this folder are the following:

-main fortran file for BI-DEM (CMP_BI.f90) 
-two module files (mod_global.f90 and mod_util.f90). 

interpolated_to_fixed_for_ICM.py:
-code snippet that needs to be integrated with ICM to interpolate the model outputs to the fixed grid points
-comments in the file give detail on each step